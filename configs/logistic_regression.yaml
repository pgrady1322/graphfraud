# ═══════════════════════════════════════════════════════════════════════
# GraphFraud — Logistic Regression Baseline Configuration
# ═══════════════════════════════════════════════════════════════════════

model:
  type: logistic_regression
  params:
    C: 1.0
    max_iter: 1000
    resample: true
    max_majority: 30000

data:
  data_dir: data/

output:
  dir: trained_models/
